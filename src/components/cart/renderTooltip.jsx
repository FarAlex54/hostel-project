import React from 'react'

const renderTooltip = (props) => (
  <Tooltip id="button-tooltip" {...props}>
    dfghfhfhf{props.plus}
  </Tooltip>
);
const renderTooltip = () => {
  return (
    <div>renderTooltip</div>
  )
}

export default renderTooltip